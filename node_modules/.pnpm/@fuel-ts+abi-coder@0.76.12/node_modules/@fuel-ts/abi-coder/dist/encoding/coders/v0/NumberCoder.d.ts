import type { EncodingOptions } from '../../../types/EncodingOptions';
import { Coder } from '../AbstractCoder';
type NumberCoderType = 'u8' | 'u16' | 'u32';
export declare class NumberCoder extends Coder<number, number> {
    length: number;
    paddingLength: number;
    baseType: NumberCoderType;
    options: EncodingOptions;
    constructor(baseType: NumberCoderType, options?: EncodingOptions);
    encode(value: number | string): Uint8Array;
    private decodeU8;
    decode(data: Uint8Array, offset: number): [number, number];
}
export {};
//# sourceMappingURL=NumberCoder.d.ts.map