import type { Assets } from '../types';
/**
 * Returns the list of assets with the icon paths 'resolved'. eg. `./eth.svg` -> `https://some-url.com/eth.svg`
 * @param assets - List of assets
 * @param basePath - Base path for the icon URLs (default: './')
 * @returns The assets with the icon paths resolved
 */
export declare function resolveIconPaths(assets: Assets, basePath?: string): {
    icon: string;
    name: string;
    symbol: string;
    networks: (import("../types").Ethereum | import("../types").Fuel)[];
}[];
//# sourceMappingURL=resolveIconPaths.d.ts.map