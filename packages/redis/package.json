{
  "name": "bako-redis",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "redis:dev:start": "make -C ./ cache-init && make -C ./ cache-wait",
    "redis:dev:stop": "make -C ./ cache-down"
  },
  "devDependencies": {
    "husky": "9.1.7"
  }
}
